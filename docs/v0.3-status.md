---
id: v0.3-status
status: active
as_of: 2026-07-31
scope: moagan v0.3 «tercera etapa» (V4 §1.5, Plan B sub-fase G onwards)
purpose: track what is implemented and what remains to land v0.3
note: |
  v0.3 picks up where v0.2 stopped. The «segunda etapa» (V4 §1.5)
  is closed at 100% — see `docs/v0.2-status.md` for the snapshot
  at `origin/main` 102ad19. v0.3 is the «tercera etapa»:
  DAG/decompose, stability, and advanced telemetry.
  This doc is the live status board for the v0.3 work; updates
  land on every commit of the `feat/v0.4-phase-g-*` branches.
---

# `moagan` v0.3 — Estado de implementación

Snapshot de lo que está en `src/` y en la documentación normativa
hoy, contrastado contra `proposal-01-concept.md` (V4 §1.5
«tercera etapa»), `proposal-02-rust.md` (T01-06 §8 + §16.4) y
`proposal-03-add-ons.md` (catálogo aditivo).

## Roadmap v0.3

| Sub-fase | Capacidad                                          | Estado     | Riesgo |
|----------|----------------------------------------------------|------------|--------|
| **G**    | DAG/decompose phase (sólo `deep`)                   | ✅ merged  | Medio  |
| **H**    | Estabilidad con perturbaciones de pesos             | ✅ merged  | Bajo   |
| **I**    | Telemetría dashboard HTTP + export + verify        | ✅ merged  | Medio  |
| **K**    | Sub-fase K — HARD_INCOMPATIBILITIES + Embedder + extra SQLite + redact categorizado + retry budget | ✅ merged | Medio  |

Las sub-fases siguen el mismo patrón que v0.2: cada una es una
rama `feat/v0.4-phase-{g,h,i}-*` con Conventional Commits y
firma GPG. El pipeline de validación es el mismo:

```bash
cargo fmt --all -- --check
cargo clippy --all-targets -- -D warnings
cargo test --all-targets
cargo build
scripts/smoke_phase_g.sh   # 17/17 OK
scripts/smoke_phase_h.sh   # 25/25 OK
scripts/smoke_phase_k.sh   # 29/29 OK
```

## Sub-fase G — Decompose phase (DAG, sólo deep)

Implementado en `feat/v0.4-phase-g-decompose` (12 commits GPG).
Cierra T01-06 §8.1 paso 3 + §16.4 + V4 §5.3 «Descomposición
condicional».

### Commits

| SHA       | Resumen |
|-----------|---------|
| `8dbbe9a` | `docs(status): close v0.2-status.md as Plan B snapshot` |
| `0fd957c` | `feat(domain): ProblemGraph + DAG types` |
| `928f724` | `feat(llm): decomposer role + decompose.md prompt` |
| `734f5b5` | `feat(phase): DecomposePhase + repair pass` |
| `59cfa3e` | `feat(pipeline): wire DecomposePhase in deep mode` |
| `6528e60` | `feat(sketch): consume problem_graph.json (DAG distribution)` |
| `12065ec` | `feat(storage): v006 problem_graphs table + mirror` |
| `8010d38` | `feat(domain): Proposal.source_nodes for coverage` |
| `a5a1d87` | `test(integration): Phase G e2e (8 scenarios)` |
| `4358af1` | `test(smoke): smoke_phase_g.sh (17 checks)` |
| `b5a9135` | `style: clippy cleanups` |

### Capacidad

- **Trigger ladder (V4 §5.3)**: `should_decompose(brief) == true`
  cuando hay ≥3 constraints, ≥3 deliverables, o magic words
  (`subproblem`, `phase`, `depends on`, `after`, `once`).
  La 5ª condición de V4 §5.3 («mix arquitectura + dominio +
  implementación») queda como follow-up del catálogo aditivo:
  requeriría añadir un campo `domain_tags: Vec<String>` a
  `Brief` y bumpear `schema_version`. Hoy los otros 3 triggers
  cubren el mismo espacio problemático desde otro ángulo.
- **Trivial path**: cuando la ladder falla, la fase escribe
  `ProblemGraph::trivial(...)` a `problem_graph.json` SIN
  gastar una llamada LLM. Tarda 0 tokens y 0 ms.
- **DAG generation**: cuando la ladder pasa, la fase llama al
  rol `decomposer` (T=0.3, top_p=0.9, max_tokens=4096) y
  parsea la respuesta como `ProblemGraph`.
- **DAG repair**: la fase valida el grafo con Kahn; un grafo
  con ciclos o dependencias colgantes se repara eliminando
  nodos ofensivos. Si todo se cae, vuelve al trivial path.
- **Sketch DAG distribution**: `SketchPhase` consume
  `problem_graph.json` cuando existe y es no-trivial. Las
  sketches se distribuyen por nodo (1 sketch por nodo o
  `ceil(count / node_count)` por nodo).
- **SQLite mirror**: la tabla `problem_graphs` (migración v006)
  registra `(run_id, brief_blake3, should_decompose, node_count,
  at_unix)` por run. Permite queries estilo `moagan inspect`
  del tipo «qué runs hicieron decompose y cuántos nodos?».
- **Wiring**: `DecomposePhase` se inserta entre `RoutePhase` y
  `SketchPhase` SOLO en `Mode::Deep`. El resto de modos no
  insertan la fase y pagan 0 overhead.

### Métricas

- **LLM calls adicionales**: 1 por deep run (la llamada al
  `decomposer`). Se omite cuando la ladder corta.
- **Test coverage**: 9 unit tests + 8 integration tests + 17
  smoke tests = 34 nuevos. Todos pasan.
- **Compat**: la migración v006 es aditiva. DBs pre-v006
  toleran el nuevo código vía `record_problem_graph` que
  detecta `user_version < 6` y retorna `Ok(())`.

### Limitaciones documentadas

1. `HARD_INCOMPATIBILITIES` (D.13.15) NO implementado — sigue
   como opt-in del catálogo aditivo. La síntesis intra-cluster
   (Phase D) no bloquea pares incompatibles.
2. ~~`Proposal.source_nodes` se rellena con la lista de nodos
   que la propuesta cubre, pero el campo todavía no se
   computa automáticamente en `ProposePhase`. El wiring del
   populate queda para una sub-fase futura.~~ **Resuelto en
   sub-fase H commit 3** (`feat(propose): populate
   Proposal.source_nodes from problem_graph.json`).
3. `SketchPhase` consume el DAG pero no paraleliza por capa
   topológica — reparte el conteo entre nodos y emite todas
   las sketches en paralelo (cap por `max_parallelism`). El
   layer-by-layer execution (esperar a que la capa N termine
   antes de emitir la N+1) queda como opt-in del catálogo.
4. `pedgraph` NO se añade como dep. La implementación de Kahn
   es manual (T01-06 §0.2 baseline).

## Sub-fase H — Estabilidad (✅ merged)

Implementado en `feat/v0.4-phase-h-stability` (10 commits GPG).
Cierra V4 §5.12 paso 6 + V4 §5.14 segundo trigger («el ranking
es inestable») + la limitación #2 de Phase G
(`Proposal.source_nodes` populate).

### Commits

| SHA       | Resumen |
|-----------|---------|
| `dbff7d6` | `chore(release): bump version to 0.3.0` |
| `0ca8f4c` | `feat(domain): Ranking.stability_score + StabilityLabel + source_nodes plumbing` |
| `5355f48` | `feat(propose): populate Proposal.source_nodes from problem_graph.json` |
| `736c45a` | `feat(ranking): stability module — perturb_weights + stability_score` |
| `b4b5c28` | `feat(config): StabilityConfig knobs (n_perturbations, sigma, threshold, seed)` |
| `2a4c498` | `feat(phase): RankPhase step 5.6 — stability check, label, sidecar fields` |
| `d387050` | `feat(checkpoint): human checkpoint trigger on Sensitive ranking (V4 §5.14)` |
| `0feaaae` | `test(integration): Phase H — 8 e2e scenarios` |
| `dcd9e9e` | `test(smoke): smoke_phase_h.sh — 25 checks` |
| `docs` | `docs(proposal): §8.4.3 + this snapshot update` |

### Capacidad

- **`Ranking.stability_score`** — `Option<HashMap<proposal_id, f32>>`
  con la fracción `[0.0, 1.0]` de perturbaciones bajo las que el
  proposal mantuvo su posición. `None` cuando el check se omitió
  (config deshabilitado, n=0, o una sola propuesta).
- **`Ranking.stability_label`** — `Option<StabilityLabel>` con
  `Stable | Sensitive` (enum serializado en lowercase por la
  convención del spec).
- **`Ranking.stability_sigma`** — `Option<f32>` con el sigma
  efectivo de la perturbación, registrado para correlación con
  la sensibilidad detectada.
- **`Config::stability`** — bloque nuevo con `enabled`,
  `n_perturbations`, `sigma_default`, `sigma_interactive`,
  `sensitive_threshold`, `seed`. Defaults conservadores que no
  perturban ganadores claros (sigma 0.05).
- **`src/ranking/stability.rs`** — funciones puras
  `perturb_weights`, `stability_score`, `stability_check`. Box-
  Muller vía `fastrand` (ya en deps); clip `[0.0, 2.0]`. Sin
  nuevas dependencias.
- **`RankPhase` paso 5.6** — corre el check post-reemplazo
  (Phase F), persiste en `rankings/ranking.json`, emite
  `tracing::info!` con el verdict.
- **V4 §5.14 trigger** — sobre `Sensitive` + `ctx.interactive`,
  abre `CheckpointKind::Custom` con la pregunta embebiendo
  `top_score`, `threshold`, `sigma`, `winner`. Default yes
  (continuar). Runs no interactivos caen en
  `<skipped:non_interactive>` (audit trail).
- **`Proposal.source_nodes` populate** — `ProposePhase::compute_source_nodes`
  lee `problem_graph.json` cuando es no-trivial y asigna a
  cada proposal los ids de los nodos cuyo Jaccard con el texto
  del proposal es `<= 0.7` (mismo umbral que
  `RankPhase::CLUSTER_THRESHOLD`). Asignación ordenada por
  `(distancia, id)` para idempotencia.

### Métricas

- **LLM calls adicionales**: 0. El check es pura cómputo
  sobre las `Aggregated` sidecars existentes.
- **Test coverage**: 10 unit tests en `stability.rs` + 3 unit
  tests en `propose.rs` + 8 integration tests en
  `tests/integration_phase_h.rs` + 25 smoke checks =
  **46 nuevos**. Todos pasan.
- **Compat**: los 3 campos nuevos en `Ranking` son
  `#[serde(default, skip_serializing_if = "Option::is_none")]`,
  así que los sidecars v0.2 (sin los campos) siguen parseando
  como `None` en cada uno. Tests
  `legacy_ranking_without_stability_fields_parses` +
  `ranking_with_stability_round_trips_json` pinan el contrato.
- **Crate version**: `Cargo.toml` bumped a `0.3.0` (cierra la
  discrepancia #2 que arrastraba v0.2-status desde `main`).
  El bump a `0.4.0` queda para cuando v0.3 (H + I + J + K) esté
  cerrada.

### Limitaciones documentadas

1. `proptest` no se añade como dep — los invariantes de la
   perturbación (clip, monotonicidad de sigma, fracciones suman
   1.0) están cubiertos por tests unitarios con seeds fijos.
   Si el repo más adelante adopta `proptest`, las versiones
   `proptest!` ya están escritas en `stability.rs` (sección
   `proptests`).
2. El mirror a SQLite del verdict de estabilidad se hace vía
   `tracing::info!` por ahora; un nuevo campo en la tabla `runs`
   puede aterrizar en sub-fase posterior (Phase I, dashboard).
3. `Resolution::Modify(text)` del checkpoint es informativo; el
   pipeline no re-rankea con la nota del usuario todavía —
   `moagan rerank` sigue siendo la vía explícita para
   re-correr el rank phase.

## Sub-fase I — Telemetría avanzada (✅ merged)

Implementada en `feat/v0.4-phase-i-telemetry` (14 commits GPG).
Cierra T01-06 §10.7-§10.10 + V4 §8.7-§8.8 + V4 §9.1-§9.2 + V4 §12.

### Commits

| SHA       | Resumen |
|-----------|---------|
| `0f225d7` | `feat(telemetry): Cmd::Telemetry + 8 subcommand stubs` |
| `ceb1a55` | `feat(storage): read-only SQLite queries for the dashboard` |
| `070a54d` | `feat(telemetry): implement 'list' subcommand` |
| `2bb3d41` | `feat(telemetry): implement 'summary' subcommand` |
| `a6a4176` | `feat(telemetry): implement 'compare' subcommand` |
| `0115804` | `feat(telemetry): implement 'provider' subcommand` |
| `1fa8cba` | `feat(telemetry): implement 'export' subcommand + SHA256SUMS archive` |
| `9eb6fe5` | `feat(telemetry): implement 'verify' subcommand + re-hash` |
| `4d298fc` | `feat(telemetry): implement 'cleanup' subcommand + retention pass` |
| `7546745` | `feat(telemetry): implement 'view' subcommand + read-only HTTP dashboard` |
| `c8abd2c` | `feat(config): server.port + retention knobs (Phase I)` |
| `cc0b262` | `test(integration): Phase I e2e scenarios (15 tests)` |
| `38f06cc` | `test(smoke): smoke_phase_i.sh with 20 checks` |
| `docs`   | `docs(proposal): expand §10.7-§10.10 + close Phase I in v0.3-status` |
| `fixes` | post-merge: smoke refs + proxy 503 race + `--archive` + `ensure_home` (see notes below). |
| `docs`   | este snapshot |

### Capacidad

**`moagan telemetry <sub>`** — los ocho subcomandos
expuestos como `Cmd::Telemetry` con dispatcher `async`
(`view::run` es la única `async`; el resto son síncronos):

- `list [--limit <N>] [--run <id>]` — tabla compacta de runs
  recientes o drill-in a uno con resumen + phases +
  provider_usage. Modo multi-run ordena por
  `created_unix DESC`.
- `summary --run <id>` — duración wall-clock + counters
  (calls, tokens, providers, phases, warnings,
  checkpoints, disk size) + secciones `by model` /
  `by phase`.
- `compare --run-a <A> --run-b <B>` — side-by-side +
  delta por métrica (`tokens`, `calls`, `ok_calls`,
  `error_calls`, `timeout_calls`, `cancelled_calls`,
  `providers`, `phases`, `warnings`, `checkpoints`,
  `duration_secs`).
- `provider [--list | --plan <name>]` — roster de
  proveedores con usage acumulado o drill-in a un plan
  (endpoint + recent usage por modelo).
- `view --port <port>` — servidor HTTP read-only sobre
  `tokio::net::TcpListener` puro (sin `axum` / `hyper` /
  `tiny_http`). Endpoints:
    - `GET /api/runs`
    - `GET /api/runs/<id>`
    - `GET /api/runs/<id>/phases`
    - `GET /api/runs/<id>/calls`
    - `GET /api/runs/<id>/provider_usage`
    - `GET /api/runs/<id>/hashes`
    - `GET /api/runs/<id>/export?level=…&format=…`
    - `GET /` (landing page)
  Loopback-only (V4 §8.8). Default port 4096;
  `pick_port` skips the blacklist `[22, 80, 443, 3306,
  5432, 6379, 8080, 8443]` and falls forward up to 1000.
  Per-request IO timeout 30s; `Connection: close` so the
  server tears down cleanly.
- `export --run <id> [--level summary|full] [--format
  tar.gz|tar|zip] [--out <path>]` — bundle el run en un
  archivo con `SHA256SUMS` canónico (formato GNU
  `sha256sum -b`). Compression `gz` para tar.gz; `deflate`
  para zip. Re-export usa `tar`/`flate2`/`zip` crates
  (ya en deps). Hash streaming con buffer 64 KiB.
- `verify --path <archive|dir>` — extrae el archivo (si es
  necesario) y re-hashea cada archivo listado en
  `SHA256SUMS`. Verdicts: `OK`, `MISSING`, `MISMATCH`.
  Devuelve `Error::InvalidState` cuando hay al menos un
  fallo (sirve como gate de CI).
- `cleanup [--dry-run]` — retention pass sobre `.runs/`.
  Tres filtros componibles (age, count, storage) +
  política `delete` o `archive`. Lee `Config::retention`
  para los knobs (`keep_runs_days: 30`,
  `keep_runs_count: 100`, `max_storage_bytes: 50 GB`,
  `policy: delete`). `--dry-run` no toca el filesystem;
  sin el flag, ejecuta la política seleccionada.

### Storage impact

Cinco queries nuevas en `src/storage/sqlite.rs`
(`Db::run_aggregate`, `Db::list_provider_usage_for_run`,
`Db::list_phase_summaries_for_run`,
`Db::aggregate_provider_usage`,
`Db::recent_runs_for_provider`). Las primeras tres son
read-only contra el esquema v006 existente — no hay
nueva migración SQLite. Los structs `RunAggregate`,
`ProviderUsageRow`, `PhaseSummaryRow`, `RunRow` y
`CallRow` derivan ahora `Serialize` (y los dos primeros
+ `RunRow` también `Deserialize` para los integration
tests).

### Config

Dos bloques nuevos en `src/config.rs`:
- `ServerConfig { port: 4096, host: "127.0.0.1",
  io_timeout_secs: 30, ensure_home: true }` — alambrado
  al dashboard.
- `RetentionConfig { keep_runs_days: 30,
  keep_runs_count: 100, max_storage_bytes: 50 GB,
  policy: "delete" }` — alambrado al cleanup.

### Métricas

- **LLM calls adicionales**: 0. Toda la fase es
  cómputo puro sobre sidecars existentes.
- **Test coverage**: 617 unit tests + 15 integration
  tests + 20 smoke checks = **652 escenarios** en
  total. Todos pasan.
- **Compat**: los sidecars JSON pre-existentes siguen
  parseando sin cambios (los nuevos campos en
  `RunAggregate` se derivan en runtime). El dashboard
  sólo expone datos que ya estaban en SQLite o en el
  filesystem — no introduce un nuevo formato de
  sidecar.

### Limitaciones documentadas

1. El dashboard sirve exclusivamente loopback
   (V4 §8.8). Para acceso remoto se necesitará un
   reverse-proxy autenticado (futuro sub-fase J o K.x).
2. BLAKE3 como algoritmo de hash para el export queda
   como opt-in (T01-06 §10.9 menciona SHA-256 como
   default canónico).

### Notas post-merge

Cuatro cierres aplicados en commits posteriores al
squash-merge inicial (todos firmados GPG en
`feat/v0.4-phase-i-telemetry`):

- `smoke_audit_proxy.sh`: 25 grep refs a `src/telemetry.rs`
  reescritas a `src/telemetry/mod.rs` (el archivo
  desapareció al modularizar la telemetría en commit
  `0f225d7`). 461/461 checks verdes.
- `audit/proxy::resolve_log_path` se dividió en dos
  variantes: `resolve_log_path` (non-blocking, usado en
  el banner de startup) y `resolve_log_path_blocking`
  (espera con backoff 50ms → 1s, deadline 10s, usado
  en cada handler de conexión). Cierra la race 503
  "no active run" sin romper el banner de los integration
  tests que tienen timeout de 5s.
- `moagan telemetry cleanup --archive`: nuevo flag CLI
  que sobreescribe `Config::retention.policy` para una
  sola invocación. `moagan telemetry view` ahora consume
  `ServerConfig::ensure_home` (cuando es `false` falla
  fast si el home no tiene `.runs/`, en vez de crear
  directorios vacíos).
- `e2e_audit_proxy.sh`: nuevo env var
  `MOAGAN_SMOKE_EXPLORE_TIMEOUT` (default 1800s) que
  controla solo el cap del run explore-mode real (los
  otros modos siguen con `MOAGAN_SMOKE_TIMEOUT`). Explore
  genera 12 sketches y cada retry puede tardar 20-60s
  con la API real, así que el cap global de 3600s puede
  apretar en runners de CI cargados. La nota sobre la
  discrepancia 47/4 (47 auditadas internas vs 4 externas)
  se documenta explícitamente: la cross-run LLM cache
  (`src/phases/phase.rs::RunContext::call`) corta antes
  del provider, así que los cache hits dejan fila en
  `calls.jsonl.gz` pero NO hacen HTTP request al proxy.
  Con un tmpdir fresco la cache está vacía y todo sketch
  es cache miss; la diferencia entre `unmatched_internal`
  y `match_count` viene entonces de retries por parse
  failures, no de cache hits.

## Conventions del repo (recordatorio)

- Conventional Commits, GPG-signed (clave `414687A3CD7E65B9`).
- Cada sub-fase cierra con validación del usuario antes del
  push + PR (pasos 4-10 del protocolo).
- No-go list aplicada vía `scripts/check-no-forbidden-crates.sh`
  + `scripts/check-no-anthropic-sdk.sh`.

## Sub-fase K — Add-ons opt-in del catálogo (✅ merged)

Implementada en `feat/v0.4-phase-k-add-ons` (5 commits GPG
nuevos sobre los 2 ya mergeados de K.1). Cierra los
siguientes patches del catálogo aditivo de `proposal-03`:

| Patch del spec | Sub-fase | Commit |
|---|---|---|
| §D.13.15 — `HARD_INCOMPATIBILITIES` constant | K.1 | `d02ba88` + `802e6ae` |
| §D.1.3 — `src/llm/embed/` con `Embedder` trait | K.2 | `861c660` |
| §D.21.6 — Per-mode retry budget matrix | K.9 | `861c660` (junto con K.2) |
| §D.5.1 — 5 tablas SQLite nuevas (outbox / redact_audit / manifest_events / process_locks / provider_rollups) | K.5 | `7696754` |
| §D.8.2 — Sustitución por categoría de patrón | K.7 | `6cf6bea` |

### Capacidad

- **`HARD_INCOMPATIBILITIES` matrix (K.1)** — constante de 10
  pares mutuamente excluyentes en `src/domain/constraint.rs`
  (`monolith`/`microservices`, `sync_rpc`/`event_driven`,
  `strong_consistency`/`eventual_consistency`, `sql`/`nosql`,
  `self_hosted`/`serverless`, `rust`/`non_permitted_runtime`,
  `single_tenant`/`multi_tenant`,
  `monolith_db`/`polyglot_persistence`,
  `pull_based`/`push_based`,
  `custom_protocol`/`standard_protocol`). Helper simétrico
  `is_incompatible(a, b)` + iterador `find_conflicts(tags)`.
  `SynthesizePhase::cluster_conflict` consulta la matriz
  antes de invocar al LLM integrador; los clusters
  incompatibles se saltan con un sidecar
  `synthesized/skipped_<NN>.json` (schema_version='v1') en
  vez de producir output incoherente.
- **`Embedder` trait + `HashingEmbedder` (K.2)** — trait
  `Embedder { embed, dim, name }` en `src/llm/embed/mod.rs`
  con un `HashingEmbedder` por defecto (256-dim, FNV-1a 32
  sobre tokens alfanuméricos lower-case, signo por el bit
  alto del hash, L2-normalización). Cache por texto verbatim
  con `parking_lot::Mutex`. Coseno helper público para
  callers que ya tienen vectores normalizados. 13 unit
  tests cubren empty / deterministic / normalised / dim /
  cache / similarity / dissimilarity + los 4 vectores
  canónicos FNV-1a (`'', 'a', 'abc', 'foobar'`).
- **Per-mode retry budget (K.9)** — `pub enum RetryReason`
  (Transport / RateLimit / Parse / Schema / Timeout /
  Truncated) + `pub struct RetryBudget { max_attempts,
  use_json_repair }` + `pub fn budget_for(Mode, RetryReason)
  -> RetryBudget` que reproduce la matriz del spec (D.21.6)
  verbatim. Por ahora es read-only — el siguiente paso de
  integración sustituirá el `n_attempts = 5` hard-coded de
  `phase.rs::call_with_retry_parse` por `budget_for(current_mode,
  current_reason)`. 10 unit tests pinea los valores del spec.
- **v008 SQLite migration (K.5)** — cinco tablas nuevas en
  `src/storage/migrations/v008_add_ons.sql`:
  `outbox_events` (D.1.4), `redact_audit` (D.8.5),
  `manifest_events`, `process_locks` (D.1.5), y
  `provider_rollups`. 9 helpers nuevos en
  `src/storage/sqlite.rs` (`record_outbox_event`,
  `list_outbox_events_for_run`, `record_redact_audit`,
  `list_redact_audit_for_run`, `record_manifest_event`,
  `acquire_process_lock`, `release_process_lock`,
  `increment_provider_rollup`, `get_provider_rollup`).
  Todos son best-effort contra `PRAGMA user_version`: en
  una DB pre-v008 son no-op, así que ningún run existente
  rompe con el nuevo código. 4 row types nuevos:
  `OutboxEventRow`, `RedactAuditRow`, `ManifestEventRow`,
  `ProviderRollupRow`. 9 unit tests cubren round-trips,
  idempotencia, y semánticas single-row del process lock.
- **`PatternKind` + `apply_with_categories` (K.7)** — enum
  `PatternKind` con 14 variantes
  (`SkCpApiKey`/`GithubPat`/`AwsAccessKey`/`Jwt`/
  `BearerHeader`/`PasswordKv`/`Email`/`PrivateIp`/
  `CreditCard`/`PrivateKey`/`ConnString`/
  `AnthropicApiKey`/`OpenaiApiKey`/`GeminiApiKey`/`Unknown`)
  + `substitute(kind)` que devuelve el marcador categorizado
  del spec + `kind_for_pattern_id(id)` que mapea cada
  pattern id legacy a su `PatternKind`. `RedactResult {
  text, kinds: Vec<(PatternKind, usize)> }` y
  `apply_with_categories(policy, surface, text)` en
  `src/redact/apply.rs` que devuelve el texto redactado Y
  los counts por kind listos para persistir en
  `redact_audit`. 5 unit tests cubren cada variante
  nombrada; los patrones sin mapeo se saltan silenciosamente
  y siguen siendo cubiertos por la `apply()` legacy.

### Métricas

- **LLM calls adicionales**: 0. K.2/K.5/K.7/K.9 son todos
  cómputo puro o persistencia; K.1 evita 1 LLM call
  (la del integrador) por cluster incompatible.
- **Test coverage**: 13 (embed) + 10 (retry_budget) + 9 (v008
  sqlite) + 5 (categorised redact) + 4 (constraint
  is_incompatible) + 5 (synthesize cluster_conflict) +
  13 (integration e2e) + 29 (smoke) = **88 tests/scenas
  nuevos**. Todos pasan.
- **Compat**: la migración v008 es aditiva. Las DBs pre-v008
  toleran el nuevo código vía el probe `user_version < 8` en
  cada helper (no-op silencioso). Los sidecars JSON
  pre-existentes siguen parseando sin cambios (los nuevos
  campos son `#[serde(default, skip_serializing_if)]`).
- **Crate version**: `Cargo.toml` queda en `0.3.0`. El bump
  a `0.4.0` se decidirá al cerrar la sub-fase K como un
  todo.

### Limitaciones documentadas

1. `RemoteEmbedder` (HTTP) y `fastembed` (BGE-small) del
   spec D.1.3 quedan como follow-up explícito — `cluster_by_embedder`
   consumirá el `HashingEmbedder` que esta sub-fase ships.
2. El retry budget es read-only: `phase.rs::call_with_retry_parse`
   sigue con `n_attempts = 5`. La integración real con la
   matriz del spec es un commit aparte.
3. Los 5 nuevos helpers SQLite son best-effort contra
   `user_version`. El siguiente sub-fase que quiera usar
   `outbox_events` o `redact_audit` en producción debe
   asumir `user_version >= 8` (lo cual es cierto para
   cualquier DB abierta con un binario v0.4).
4. El worker del outbox (D.1.4 §1.2: «flush cada 5s») y el
   lease module (D.1.5) son follow-ups explícitos. Esta
   sub-fase deja la tabla y los helpers; el worker se
   implementa cuando haya un consumidor concreto.
5. `PatternKind` no incluye todavía `AwsSecretKey`,
   `SlackToken`, `StripeLive`, `StripeTest`, `Sendgrid`,
   `Twilio`, `GoogleOAuthRefresh`, `SsnLike`,
   `MoaganMinimaxEndpoint` (todos los patterns legacy de
   `src/redact/patterns.rs` que no están en el catálogo
   categorizado del spec). Se mantienen cubiertos por la
   `apply()` legacy; el siguiente paso es mapear cada uno
   a su `PatternKind` correspondiente cuando el spec
   extienda la tabla.
