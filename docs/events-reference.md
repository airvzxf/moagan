# Events reference — `moagan` NDJSON stream

> Auto-generated by `moagan-docgen events`. Do not edit by hand — run the docgen tool and commit the diff.

Schema version: **1** (bumped on any backwards-incompatible change to the `Event` enum). Additive changes keep the same version.

The `moagan` binary emits typed domain events on **stdout** as NDJSON (one JSON object per line). The stream is silenced when stdout is a TTY and re-enabled when stdout is redirected or piped. See `--event-format` and `MOAGAN_EVENT_FORMAT` for controls.

## Activation

```bash
moagan run --mode fast --provider mock:mock-model --prompt 'x' > events.jsonl
jq -c 'select(.kind == "run_end")' events.jsonl
```

## Event kinds

| `kind` | When emitted | Notable fields |
|---|---|---|
| `run_start` | At run start, after the dispatcher resolves the command. | `run_id`, `mode`, `provider`, `model`, `prompt_hash` |
| `run_end` | At run end (success or error). | `run_id`, `status`, `exit_code`, `elapsed_ms`, `artefacts` |
| `phase_start` | At the start of every `Phase::execute`. | `phase`, `seq` |
| `phase_end` | On successful `Phase::execute` completion. | `phase`, `seq`, `elapsed_ms`, `status` |
| `phase_error` | When a `Phase::execute` returns `Err`. | `phase`, `seq`, `error`, `exit_code` |
| `llm_call` | On successful `provider.send` (non-probe). | `call_id`, `phase`, `role`, `provider`, `model`, `elapsed_ms`, `ok`, `input_tokens`, `output_tokens`, `retry_count` |
| `discovery_iteration` | Per sketch loop iteration in discovery. | `n`, `total`, `section`, `model`, `cell_dim`, `cell_facet`, `temperature`, `replica`, `sketch_index`, `outcome` |
| `probe` | Per auto-probe call (temperature / max_tokens). | `probe_kind`, `candidate`, `iteration`, `provider`, `model`, `outcome` |
| `warning` | When `Telemetry::warn` is called. | `code`, `level`, `phase`, `details` |
| `decision` | At curated decision points throughout the pipeline (verbosity controlled by `--decision-format`). | `decision_kind`, `payload` |

## Variant schema

### `RunStart` (`kind = "run_start"`)

| Field | Type |
|---|---|
| `schema` | `u32` |
| `ts` | `String` |
| `run_id` | `&'a str` |
| `mode` | `&'a str` |
| `provider` | `&'a str` |
| `model` | `&'a str` |
| `prompt_hash` | `&'a str` |

### `RunEnd` (`kind = "run_end"`)

| Field | Type |
|---|---|
| `schema` | `u32` |
| `ts` | `String` |
| `run_id` | `&'a str` |
| `status` | `&'a str` |
| `exit_code` | `i32` |
| `elapsed_ms` | `u64` |
| `artefacts` | `serde_json::Value` |

### `PhaseStart` (`kind = "phase_start"`)

| Field | Type |
|---|---|
| `schema` | `u32` |
| `ts` | `String` |
| `phase` | `&'a str` |
| `seq` | `i64` |

### `PhaseEnd` (`kind = "phase_end"`)

| Field | Type |
|---|---|
| `schema` | `u32` |
| `ts` | `String` |
| `phase` | `&'a str` |
| `seq` | `i64` |
| `elapsed_ms` | `u64` |
| `status` | `&'a str` |

### `PhaseError` (`kind = "phase_error"`)

| Field | Type |
|---|---|
| `schema` | `u32` |
| `ts` | `String` |
| `phase` | `&'a str` |
| `seq` | `i64` |
| `error` | `&'a str` |
| `exit_code` | `i32` |

### `LlmCall` (`kind = "llm_call"`)

| Field | Type |
|---|---|
| `schema` | `u32` |
| `ts` | `String` |
| `call_id` | `&'a str` |
| `phase` | `&'a str` |
| `role` | `&'a str` |
| `provider` | `&'a str` |
| `model` | `&'a str` |
| `elapsed_ms` | `u64` |
| `ok` | `bool` |
| `input_tokens` | `u32` |
| `output_tokens` | `u32` |
| `retry_count` | `u32` |

### `DiscoveryIteration` (`kind = "discovery_iteration"`)

| Field | Type |
|---|---|
| `schema` | `u32` |
| `ts` | `String` |
| `n` | `usize` |
| `total` | `usize` |
| `section` | `&'a str` |
| `model` | `&'a str` |
| `cell_dim` | `&'a str` |
| `cell_facet` | `&'a str` |
| `temperature` | `f32` |
| `replica` | `usize` |
| `sketch_index` | `usize` |
| `outcome` | `&'a str` |

### `Probe` (`kind = "probe"`)

| Field | Type |
|---|---|
| `schema` | `u32` |
| `ts` | `String` |
| `probe_kind` | `&'a str` |
| `candidate` | `f32` |
| `iteration` | `u32` |
| `provider` | `&'a str` |
| `model` | `&'a str` |
| `outcome` | `&'a str` |

### `Warning` (`kind = "warning"`)

| Field | Type |
|---|---|
| `schema` | `u32` |
| `ts` | `String` |
| `code` | `&'a str` |
| `level` | `&'a str` |
| `phase` | `Option<&'a str>` |
| `details` | `serde_json::Value` |

### `Decision` (`kind = "decision"`)

| Field | Type |
|---|---|
| `schema` | `u32` |
| `ts` | `String` |
| `decision_kind` | `&'a str` |
| `payload` | `serde_json::Value` |

## Decision-event verbosity (`--decision-format`)

Decision events are emitted independently of the rest of the bus and have their own verbosity knob. Each curated `decision_kind` string is classified as either **Summary** (always visible) or **AllOnly** (visible only under `--decision-format all`).

```bash
moagan … --decision-format summary   # default; curated set only
moagan … --decision-format all      # everything (dashboards, audits)
moagan … --decision-format off      # silence every Decision event
MOAGAN_DECISION_FORMAT=all moagan … # env var (same precedence as flag)
```

Resolution order (highest first):

1. `MOAGAN_DECISION_FORMAT` env var (`off` / `summary` / `all`; unknown values fall back to `summary`).
2. Explicit `--decision-format` flag.
3. Default (`summary`).

### Curated `decision_kind` strings

| `decision_kind` | Level |
|---|---|
| `winner_picked` | Summary |
| `low_confidence_winner` | Summary |
| `cluster_skipped` | Summary |
| `repair_applied` | Summary |
| `portfolio_finalized` | Summary |
| `category_assigned` | AllOnly |
| `judge_verdict` | AllOnly |
| `cache_hit` | AllOnly |
| `cache_miss` | AllOnly |

The classification is exposed as `moagan::telemetry::stdout_events::DECISION_KIND_INVENTORY`. Unknown `decision_kind` strings default to Summary (every kind is visible until classified).


## Generated by

`moagan-docgen events` (EPIC #852). Parses `src/telemetry/stdout_events.rs` to extract the `Event<'a>` variant registry; consumes `DECISION_KIND_INVENTORY` for the curated decision table.
