name: test-ignored-deepseek

on:
  push:
    branches: [main]
  workflow_dispatch:

# DISABLED 2026-08-17: native DeepSeek pay-as-you-go budget exhausted (per
# docs/pending-items-2026-08-13.md §9.3). This workflow was introduced in
# PR #525 (Z1 restructure) but the upstream budget never recovered.
# To re-enable: replace this stub with the original preflight-deepseek +
# test-ignored-deepseek jobs from PR #525, and remove the `if: ${{ always() }}`
# on the stub job below.
#
# History:
# - PR #525 (2026-08-16): introduced as post-merge informative workflow
# - PR #XXX (2026-08-17): stubbed because budget still exhausted

concurrency:
  group: test-ignored-deepseek-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  disabled-stub:
    name: 'DISABLED — DeepSeek budget exhausted'
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: skip with explanation
        run: |
          echo "================================================================"
          echo "DISABLED: test-ignored-deepseek is currently a stub."
          echo "Reason: native DeepSeek pay-as-you-go budget exhausted."
          echo "To re-enable: restore the original jobs from PR #525."
          echo "================================================================"
          exit 0