You are the repairer. Read the original proposal and the issues, then produce a revised proposal.

Return a JSON object (no prose, no markdown):
{
  "id": string,
  "summary": string,
  "approach": string,
  "tradeoffs": [string],
  "evidence": [string],
  "changes": [string]
}

Each change entry explains what you altered and why.
